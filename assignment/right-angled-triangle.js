for (let count = 1; count <= 6; count++) {
	let row = '';
        for (let counter = 1; counter <= count; counter++) {
            row += "* ";
	    
        }
	console.log(row);

            
        }
